# 📱 车辆速度检测App - 云打包完整指南

## 🎯 概述

本项目是一个基于Kivy和YOLOv8的车辆速度检测应用，支持打包成离线Android/iOS应用。本指南提供了多种云打包方案，无需本地配置复杂的Android开发环境。

---

## 📚 文档导航

- **快速开始** → 查看 `云打包快速开始.md`（5分钟上手）
- **详细指南** → 查看 `云打包指南.md`（完整教程）
- **本文档** → 方案对比和选择建议

---

## 🚀 推荐方案对比

| 方案 | 难度 | 成本 | 构建时间 | 推荐度 | 适用场景 |
|------|------|------|----------|--------|----------|
| **GitHub Actions** | ⭐⭐ | 免费 | 20-40分钟 | ⭐⭐⭐⭐⭐ | 公开仓库，最推荐 |
| **GitLab CI/CD** | ⭐⭐ | 免费 | 20-40分钟 | ⭐⭐⭐⭐ | GitLab用户 |
| **Docker本地** | ⭐⭐⭐ | 免费 | 20-40分钟 | ⭐⭐⭐ | 不想用云服务 |
| **AppVeyor** | ⭐⭐ | 免费/付费 | 15-30分钟 | ⭐⭐⭐ | Windows构建 |
| **CircleCI** | ⭐⭐ | 免费/付费 | 15-30分钟 | ⭐⭐⭐ | 企业用户 |

---

## ✅ 方案一：GitHub Actions（最推荐）

### 为什么选择？

- ✅ **完全免费**（公开仓库）
- ✅ **自动化**：代码推送自动构建
- ✅ **易于使用**：配置文件已准备好
- ✅ **支持缓存**：后续构建更快
- ✅ **多架构支持**：arm64-v8a, armeabi-v7a

### 快速开始

1. **推送代码到GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/你的用户名/仓库名.git
   git push -u origin main
   ```

2. **触发构建**
   - 进入GitHub仓库 → Actions标签
   - 点击 "Build Android APK"
   - 点击 "Run workflow"

3. **下载APK**
   - 等待构建完成（首次30-40分钟）
   - 在Artifacts中下载APK

### 配置文件

已创建：`.github/workflows/build-apk.yml`

---

## ✅ 方案二：GitLab CI/CD

### 适用场景

- 使用GitLab托管代码
- 需要私有仓库的免费CI/CD

### 快速开始

1. **推送代码到GitLab**
   ```bash
   git remote add gitlab https://gitlab.com/你的用户名/仓库名.git
   git push gitlab main
   ```

2. **自动构建**
   - GitLab会自动检测 `.gitlab-ci.yml`
   - 在CI/CD → Pipelines查看进度

3. **下载APK**
   - 构建完成后在Artifacts中下载

### 配置文件

已创建：`.gitlab-ci.yml`

---

## ✅ 方案三：Docker本地构建

### 适用场景

- 不想使用云服务
- 需要完全控制构建过程
- 本地有Docker环境

### 快速开始

```bash
# 1. 构建Docker镜像
docker build -t kalman-builder .

# 2. 运行构建
docker run --rm -v "$(pwd):/app" kalman-builder

# 3. 在 Kalman/bin/ 目录找到APK
```

### 配置文件

已创建：`Dockerfile`

---

## 📋 构建前检查清单

在开始构建前，请确认：

- [ ] `yolov8n.pt` 文件存在于项目根目录
- [ ] `buildozer.spec` 配置正确
- [ ] 所有Python文件已提交
- [ ] `requirements.txt` 包含所有依赖
- [ ] GitHub/GitLab仓库已创建

---

## 🔧 构建配置说明

### buildozer.spec 关键配置

```ini
# 应用信息
title = 车辆速度检测
package.name = vehiclespeed
version = 0.1

# 包含模型文件
source.include_exts = py,png,jpg,jpeg,atlas,json,txt,pt
include.files = yolov8n.pt

# Android权限
android.permissions = CAMERA,INTERNET,WRITE_EXTERNAL_STORAGE,READ_EXTERNAL_STORAGE

# 支持的架构
android.archs = arm64-v8a, armeabi-v7a

# 依赖包
requirements = python3,kivy,opencv,numpy,networkx,ultralytics,pillow,pyjnius,android,openssl
```

---

## ⚠️ 常见问题

### Q1: 构建失败 - 找不到yolov8n.pt

**解决：**
- 确保 `yolov8n.pt` 在项目根目录（与 `app.py` 同级）
- 检查 `buildozer.spec` 中 `include.files = yolov8n.pt`

### Q2: 构建时间太长

**优化：**
- 使用缓存（GitHub Actions已配置）
- 只构建一个架构：`android.archs = arm64-v8a`
- 首次构建较慢是正常的（需下载SDK）

### Q3: APK文件太大

**优化：**
- 使用 `buildozer android release`（自动优化）
- 检查是否包含不必要的文件
- 移除 `data/` 目录（已在配置中排除）

### Q4: 如何签名APK？

**步骤：**
1. 生成密钥库：
   ```bash
   keytool -genkey -v -keystore my-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias my-key-alias
   ```

2. 在 `buildozer.spec` 中配置：
   ```ini
   android.keystore = my-release-key.jks
   android.keystore_password = 你的密码
   android.keyalias = my-key-alias
   android.keyalias_password = 你的密码
   ```

3. 构建发布版本：
   ```bash
   buildozer android release
   ```

---

## 📊 构建时间参考

| 阶段 | 首次构建 | 后续构建（有缓存） |
|------|----------|-------------------|
| 下载依赖 | 10-15分钟 | 2-3分钟 |
| 编译Python | 5-10分钟 | 3-5分钟 |
| 编译C扩展 | 10-15分钟 | 5-8分钟 |
| 打包APK | 5-10分钟 | 3-5分钟 |
| **总计** | **30-50分钟** | **13-21分钟** |

---

## 🎯 推荐流程

### 第一次使用

1. ✅ 阅读 `云打包快速开始.md`
2. ✅ 选择GitHub Actions方案
3. ✅ 推送代码到GitHub
4. ✅ 等待首次构建完成
5. ✅ 下载并测试APK

### 日常使用

1. ✅ 修改代码
2. ✅ 推送到GitHub
3. ✅ 自动触发构建
4. ✅ 下载新APK

---

## 📞 获取帮助

- 📖 详细文档：`云打包指南.md`
- 🚀 快速开始：`云打包快速开始.md`
- 🐛 问题反馈：GitHub Issues
- 📚 Buildozer文档：https://buildozer.readthedocs.io/

---

## 🎉 开始构建

选择最适合你的方案，开始构建你的第一个APK吧！

**推荐：从GitHub Actions开始** → 查看 `云打包快速开始.md`

