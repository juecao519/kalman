# 🚀 如何打包 - 详细步骤

## 📍 你的GitHub仓库
**地址：** https://github.com/juecao519/kalman.git

---

## 方法一：直接推送Kalman目录内容（推荐）

如果你的GitHub仓库是空的，直接把Kalman目录下的所有文件推送到仓库根目录。

### 步骤：

```bash
# 1. 进入Kalman目录
cd Kalman

# 2. 初始化git
git init

# 3. 添加所有文件
git add .

# 4. 提交
git commit -m "Add vehicle speed detection app"

# 5. 添加远程仓库
git remote add origin https://github.com/juecao519/kalman.git

# 6. 推送到GitHub
git branch -M main
git push -u origin main
```

**注意：** 如果提示远程仓库已存在，执行：
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### ⚠️ 重要：需要修改GitHub Actions路径

如果直接推送Kalman目录内容到根目录，需要修改 `.github/workflows/build-apk.yml`：

将：
```yaml
working-directory: ./Kalman
path: Kalman/bin/*.apk
```

改为：
```yaml
working-directory: .
path: bin/*.apk
```

---

## 方法二：保持Kalman目录结构

如果你想保持Kalman目录结构（推荐用于组织多个项目）。

### 步骤：

```bash
# 1. 在项目根目录（Kalman的上一级）执行
cd ..

# 2. 初始化git
git init

# 3. 添加Kalman目录
git add Kalman/

# 4. 提交
git commit -m "Add vehicle speed detection app"

# 5. 添加远程仓库
git remote add origin https://github.com/juecao519/kalman.git

# 6. 推送到GitHub
git branch -M main
git push -u origin main
```

这样GitHub Actions工作流不需要修改，可以直接使用。

---

## 🎯 推荐方案：方法一（简单直接）

我建议使用方法一，并修改GitHub Actions配置。让我为你创建一个适配的配置文件：

